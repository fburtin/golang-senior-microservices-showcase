apiVersion: v1
kind: Service
metadata:
  name: go-showcase-api-service

spec:
  selector:
    app: go-showcase-api

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

  type: ClusterIP